# Fonts

`LXGWWenKai-Regular.ttf` is the preferred bundled font for Chinese UI text.

The runtime also checks for `ZCOOLXiaoWei-Regular.ttf`, `NotoSansCJKsc-Regular.otf`, and common system CJK fonts as fallbacks.
